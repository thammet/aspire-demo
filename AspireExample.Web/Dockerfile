FROM node:23

ARG PLAYER_API
ARG TEAM_API

ENV PLAYER_API=$PLAYER_API
ENV TEAM_API=$TEAM_API

COPY . /app
WORKDIR /app

RUN npm i 
RUN npm run build


#EXPOSE 3000
#ENV PORT=3000
#ENV HOSTNAME="0.0.0.0"

CMD ["npm", "start"]